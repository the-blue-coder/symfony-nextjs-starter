name: [project-slug]

services:
  postgres:
    restart: unless-stopped
    volumes:
      - /var/data/postgres:/var/lib/postgresql/data

  redis:
    restart: unless-stopped

  backend:
    restart: unless-stopped
    ports:
      - "[PROD_BACKEND_PORT]:80"
    volumes: []
    build:
      target: prod
