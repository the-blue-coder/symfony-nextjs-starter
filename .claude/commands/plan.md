---
description: "Interactive feature planning — clarifies requirements against project specs, writes a feature spec file, updates the progress tracker"
argument-hint: "<feature description>"
allowed-tools:
  - Read
  - Write
  - Bash
  - Glob
  - Grep
---

Argument: `$ARGUMENTS`

Read `.context/commands/plan.md` and follow its instructions exactly, using the above as the `$ARGS` value.
